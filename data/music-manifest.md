# Lambda // Music Library — Manifest

The engine reads the json block below. Each slug must match
`data/music/<slug>.md`.

```json
{
  "library": "Lambda // Music Library",
  "tagline": "Artist reports. Cool finds on file.",
  "entries": [
    "the-secession"
  ]
}
```
